#include<stdio.h>
int main(){
    printf("size of int: %zu bytes\n", sizeof(int));
    printf("size of int: %zu bytes\n", sizeof(char));
    printf("size of int: %zu bytes\n", sizeof(float));
    return 0;
}