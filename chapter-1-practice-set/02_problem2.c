#include <stdio.h>

int main(){
    /*
    calculate the area of a circle and modify the same program to the calculator the volume od a cylinder given its radius and height.
    area of circle
    A=πr2
    */
    int r = 6;
    printf("The area pf circle with the radius %d is %f", r, 3.14*r*r);
    return 0;
}